/**
 * leet - change the letters to numbers
 * @s: pointer to the string
 *
 * Return: they return the string to number
 */
char *leet (char *arr)
{
	int a, b;

	for (a = 0; arr[a] != '\0'; a++)
	{
		for (b = 0; arr[b])

	}

	return (s);
}
