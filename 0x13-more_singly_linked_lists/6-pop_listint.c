#include "lists.h"

/**
 * pop_listint - deletes the head node of a listint_t, and returns the head node’s data (n).
 * @head: contents of linked list starting from the given node
 *
 * Return: the head node’s data (n).
 *
 */
int pop_listint(listint_t **head)
{


	return(head->n);
}
