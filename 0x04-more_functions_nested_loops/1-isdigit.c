/**
 * _isdigit - check if the code is a digit or not
 * @c: they return a character
 *
 * Return: the value who introduce.
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (c = 1);
	}
	else
	{
		return (0);
	}
	return (n);
}
