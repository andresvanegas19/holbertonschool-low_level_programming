#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a;

	for (a = '0'; a <= '9'; ++a)
	{
		if (a <= 8)
		{	
		  putchar('0'+a);
			putchar(',');
		}
	}

	putchar('\n');
	return (0);
}
